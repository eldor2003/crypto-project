import {Box, Typography} from "@mui/material";
import ball from '/src/svg/ball.svg';
const Ball = () => {
  return (
    <Box
      sx={{
        // backgroundColor: 'red'
      }}
    >
    <img src={ball} />
    </Box>
  )
}

export default Ball;